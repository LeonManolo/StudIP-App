import 'package:flutter/material.dart';

class AdaptiveText extends StatelessWidget {
  const AdaptiveText({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("");
  }
}
